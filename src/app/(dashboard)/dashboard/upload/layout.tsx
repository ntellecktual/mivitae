import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Upload Resume — mivitae",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
