import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Refer & Earn — mivitae",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
