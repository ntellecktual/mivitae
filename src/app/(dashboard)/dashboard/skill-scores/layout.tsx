import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Skill Scores — mivitae",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
